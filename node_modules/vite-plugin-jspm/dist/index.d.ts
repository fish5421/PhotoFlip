import { Generator, GeneratorOptions } from '@jspm/generator';
import { Plugin } from 'vite';

type PluginOptions = GeneratorOptions & {
    downloadDeps?: boolean;
    debug?: boolean;
};
declare let generator: Generator;
declare function plugin(pluginOptions?: PluginOptions): Promise<Plugin[]>;

export { plugin as default, generator };
